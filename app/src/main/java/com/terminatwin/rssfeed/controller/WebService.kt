package com.terminatwin.rssfeed.controller

import com.loopj.android.http.RequestParams
import com.terminatwin.rssfeed.model.ActionListener
import java.io.File

class WebService : WebServiceClient() {

    //region - Authentication
    /*fun signup(listener: ActionListener, params: RequestParams, picture: File) {
        sendPicture(Settings.Url.SIGNUP, params, "", "picture", null, listener)
    }*/
}
